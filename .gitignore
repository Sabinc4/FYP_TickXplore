# Environment variables (never commit secrets)
.env
.env.local
.env.*.local
!.env.example

# Firebase service account (secret - never commit)
Back_End/config/serviceAccountKey.json

node_modules/
dist/
logs/