# Backend API base URL
VITE_API_URL=http://localhost:3001

# Hugging Face chatbot - DO NOT commit a real key.
# The recommended approach is to proxy this through your backend so the key
# never ships to the browser. If no key is set, the chatbot gracefully degrades.
VITE_HF_API_KEY=